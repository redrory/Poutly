<%= form_for (@client) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<div class="field">
		<%= f.text_field :project_name, placeholder: "Name of project" %>
	</div>

	<%= f.submit "Post", class: "btn btn-large btn-primary"%>
<% end %>
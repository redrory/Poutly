<% provide(:title, @user.name + " Profile") %>

<div class = "row">
	<aside class = "span4">
		<section>
			<h1>
				<%= @user.name %>
			</h1>
				<%= @user.email %>
		</section>

	</aside>
</div>


